import Foundation

/// Umbrella for the seemd rendering/document core.
/// Pure logic lives here so it is verifiable via `swift run seemd-selftest`
/// (XCTest is unavailable under Command Line Tools).
public enum SeemdCore {
    public static let version = "0.1.0"
}
